# Что НЕ отправлять в build-контекст backend-образа.
# ВАЖНО: .mvn/ и mvnw НЕ исключаем — они нужны для сборки в образе.
target/
*.class
hs_err_pid*

# Git / IDE / доки — на сборку не влияют.
.git/
.gitignore
.idea/
.vscode/
*.iml
README.md
Dockerfile
.dockerignore
