.git/
target/
# ВНИМАНИЕ: .mvn/ НЕ исключаем — Dockerfile копирует его (COPY .mvn/ .mvn/) ради
# maven-wrapper.properties. Маска *.jar ниже всё равно отсекает maven-wrapper.jar
# (он gitignored и докачивается mvnw через curl внутри образа).
*.jar
.DS_Store
