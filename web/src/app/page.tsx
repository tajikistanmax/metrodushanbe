import HomeClient from "@/components/HomeClient";

/**
 * Главная страница публичного портала: карта сети, список станций,
 * легенда и индикатор источника данных. Вся интерактивность — в HomeClient.
 */
export default function Home() {
  return <HomeClient />;
}
