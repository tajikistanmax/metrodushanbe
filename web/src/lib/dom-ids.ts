/**
 * Стабильные id разметки портала.
 *
 * Раньше у каждого раздела был свой id основного контейнера («route-content»,
 * «news-content», «fares-content», …) и свой skip-link рядом с собственной
 * копией шапки. Каркас теперь общий (app/layout.tsx), а значит и цель
 * skip-link должна быть одна: иначе одна ссылка не может вести в контент
 * любой страницы.
 */

/**
 * id элемента <main> каждой страницы портала. Одновременно:
 *  - цель общего skip-link (PortalSkipLink);
 *  - fallback-цель фокуса после закрытия последнего баннера уведомлений
 *    (AlertsBanner) — поэтому на главной у <main> есть ещё и tabIndex={-1}.
 */
export const MAIN_CONTENT_ID = "main-content";

/**
 * id панели со списком станций (list-mode, A11Y-06). Карта никогда не является
 * единственным способом получить информацию о сети, поэтому на главной есть
 * отдельный переход к списку — сверх общего skip-link.
 */
export const STATION_LIST_ID = "station-list";
